{
    "name": "Video to GIF",
    "dockerComposeFile": "./docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "charliermarsh.ruff"
            ]
        }
    },
    "mounts": [
        "source=${localEnv:HOME}/.claude,target=/home/peon/.claude,type=bind"
    ]
}
